# 原理 
- 选择排序的原理其实就是**找位置+移动+插入**
- 将数组分为已排序区和未排序区，每轮取未排序区的第一个元素，找到他的插入位置，后移此位置后面的元素(一边比较一边移动)，然后将这个元素插入

# 优点
- 简单
- 稳定
- 当数据本身有序的时候：不需要后移+插入，时间复杂度可以可以做到O(1)
- 原地排序
- 适合小数据：当数据(n<1000)很小的时候，效率甚至高于快速排序

# 缺点
- 效率低：时间复杂度平均是O(n^2)，尤其当整个数组逆序的时候，效率更是低的没边
- 不适合大数据：数据越大，时间复杂度O(n^2)带来的影响越致命
- 元素移动的次数过多：